# Development Log 2026-09-08

## 今日目标

- [x] 在桌面建立统一项目工作区并接入 GitHub 仓库。
- [x] 创建 Python 3.12 项目虚拟环境。
- [x] 安装并验证 OpenCV、NumPy 与 ArUco 模块。
- [x] 建立可重复运行的环境检查、摄像头探测和 Marker 检测工具。
- [x] 生成四角校准 Marker 的独立 PNG 与 A4 实际尺寸打印稿。
- [x] 完成四角校准、透视矫正与 Camera → Game 归一化坐标的软件模块。
- [x] 完成彩色路径 HSV 分割、去噪与 Path Mask 输出的软件模块。
- [x] 完成 Marker、校准和路径的统一 Unity JSON 数据包软件闭环。
- [x] 锁定无贴纸轮廓 Token 的几何编码和实物验收规则。
- [ ] 连接 DJI Pocket 3 或 iPhone 13，并记录实际视频输入参数。
- [ ] 按100%比例打印 Marker，测量50 mm校验线。
- [ ] 搭建临时平面并拍摄现场照片。

## 自动化搭建结果

- 项目路径：`C:\Users\23902\Desktop\urban-wildlife-tangible-unity`
- Python：3.12.10，使用仓库内 `.venv`。
- 视觉依赖：`opencv-contrib-python==4.14.0.94`、`numpy==2.4.6`。
- OBS：检测到 `D:\obs-studio\bin\64bit\obs64.exe`。
- OBS 文件版本：32.0.4。
- Marker 字典：ArUco `DICT_4X4_50`。
- 四角顺序：左上0、右上1、右下2、左下3。
- 卡片尺寸：70 × 70 mm，编码区50 × 50 mm，300 dpi。
- 自动化测试：34项全部通过，包括中文路径图像读写、透视场景四角识别、标准化坐标映射、HSV Path Mask去噪、数据契约字段、统一数据包原子写入、轮廓编码唯一性、V0.1圆片、V0.2造型Token和轮廓版完整数据包。
- Marker 自检：生成的 ID 0 图片被正确识别，中心为 `(413, 413)` 像素，角度为 `0°`。
- 打印稿自检：渲染后的A4页面一次识别出 ID 0、1、2、3，四个角均未出现排版遮挡。
- 校验图与检测结果保存在 [`docs/images/vision/aruco-sheet-validation/`](../images/vision/aruco-sheet-validation/)；正式打印稿位于 [`hardware/printables/aruco-four-corner-calibration-a4.pdf`](../../hardware/printables/aruco-four-corner-calibration-a4.pdf)。
- 摄像头输入0：可输出1920 × 1080，但本次实测仅约2.45 fps，不适合作为当前30 fps基线。
- 摄像头输入1：可输出1920 × 1080，本次实测约30.33 fps，可用于临时测试；仍需在 OBS 中人工确认其实际设备名称。
- Windows 当前只明确列出 `Integrated Camera`；DJI Pocket 3 尚未被系统识别为已连接设备。

## 今日必须留下的现场证据

自动化部分已经完成，但以下内容必须由真实设备产生，不能用模拟结果代替：

1. OBS 或摄像头测试截图。
2. 摄像头型号、输入编号、实际分辨率与实际帧率。
3. 打印后的50 mm校验线实测结果。
4. 临时平面和摄像头位置照片。
5. 反光、阴影、遮挡或画面抖动等失败记录。

## 下一步

等纸质 Marker 和板面可用后，使用固定俯拍画面运行 `vision/calibrate_corners.py --camera <编号>`，保存第一份真实四角校准结果；随后评估单个 Marker 的识别距离、角度和稳定性。

## 电子屏幕替代测试 01

- 输入方式：摄像头画面通过 OBS 虚拟摄像头作为 `camera 1` 输入。
- 画面尺寸：1920 × 1080。
- 检测数量：1。
- Marker ID：0，正确。
- 中心位置：`x = 963.25 px`，`y = 401.25 px`。
- 图像平面角度：`-82.5°`，与手机接近旋转90°的状态一致。
- 结论：单 Marker 的 ID、位置与角度输出链路已在真实摄像头画面中跑通。
- 限制：手机由手持支撑且与摄像头不平行，存在明显透视变形；该结果只作为软件链路证据，不代替纸质 Marker、固定俯拍和四角校准测试。
- 隐私处理：标注帧包含手部及环境背景，只保存在被 `.gitignore` 排除的 `data/raw/`，不上传 GitHub。

## 四角校准软件预验证

- 新增 `vision/calibrate_corners.py`，按左上0、右上1、右下2、左下3检查必需 Marker。
- 以四个 Marker 中心定义有效游戏区域，计算 Camera → Board 单应性矩阵。
- 输出四角标注图、900 × 600俯视矫正图和可复用的 `calibration.json`。
- 标准化坐标为左上原点、X向右、Y向下、两轴范围0–1；Unity 平面使用 X/Z 对应。
- 自动测试覆盖透视画面四 Marker 识别、四角映射、中心点映射和缺失 Marker 报错。
- 电子打印稿验证图保存在 [`docs/images/vision/four-corner-calibration-validation/`](../images/vision/four-corner-calibration-validation/)，可安全上传且不含现场隐私画面。
- 当前结论仅证明软件闭环可用；正式纸张、60 × 90 cm板面和固定俯拍设备到位后，仍需保存一次真实校准参数并检查反光、遮挡和边缘识别。

## 彩色路径软件预验证

- 新增 `vision/detect_path.py` 和 `vision/config/path_detection.json`。
- 提供亮洋红、青色和橙色三组独立 HSV 初始阈值，默认值仍为亮洋红。
- 处理链路为 HSV 阈值分割 → 开运算去除散点 → 闭运算连接小缺口 → 删除小面积连通区域。
- 输出二值 Path Mask、彩色叠加检查图，以及检测面积、占比、连通区域和边界框JSON。
- 自动测试覆盖路径保留、噪点与灰色元素排除、颜色预设隔离、多 HSV 区间合并和错误预设提示。
- 使用现有黑色板面效果图和青色预设完成端到端验证：输出1个连续区域、面积12,905 px，绿色林地、木质 Token 和黑灰背景均未进入 Mask。
- 无隐私验证结果保存在 [`docs/images/vision/path-mask-validation/`](../images/vision/path-mask-validation/)；本次使用青色只是因为现有效果图采用青色路径，不代表已经替换默认的亮洋红候选。
- 当前电子效果图仅用于验证软件流程；实体彩带到货后仍需在固定曝光、白平衡和双侧灯光下重新取样并调整阈值。

## 统一布局数据包

- 新增 `vision/build_layout_packet.py`，从同一确认帧完成四角校准、Token 识别、路径分割和归一化输出。
- 四角 ID 0–3只参与校准，不进入 `tokens`；ID 10–12映射为 `food_hotspot`，20–21映射为 `woodland`。
- Token 位置与角度均转换至校正后的板面坐标；路径输出为从左至右采样的标准化折线，符合 S001 单路径、不自交约束。
- `latest_layout.json` 使用临时文件加原子替换，Unity 不会读取到只写了一半的数据；每个周期同时按 Session、Cycle 和时间戳归档。
- 数据契约固定为 `data/schemas/layout_packet_v0.1.schema.json`，并保留版本、UTC时间、场景和校准编号。
- 新增不含个人与现场信息的透视测试夹具，用于验证5个 P0 Token、四角 Marker 和亮洋红路径的完整闭环。
- 端到端夹具结果正确输出5个已分类 Token 和11点标准化路径，所有 Token 均位于0–1边界内，路径为单一连续区域。
- 输入、标注图、Path Mask、校准数据、最新数据包及周期归档保存在 [`docs/images/vision/layout-packet-validation/`](../images/vision/layout-packet-validation/)。

## Scope 变更：操作 Token 改为轮廓识别

- 玩家操作的Food与Woodland Token取消顶部白色标签和ArUco贴纸；四角校准ArUco保持不变，但改为独立硬卡磁吸固定。
- 现有Food 60 mm圆片和Woodland 50 mm中央木座尺寸不变，避免推翻已购买材料和整体比例。
- 每个木片设置一个10 × 7 mm大方向缺口，以及位于相对90°、180°、270°的8 × 5 mm小编码槽A/B/C。
- A/B/C按1/2/4计数，逻辑ID 10、11、12、20、21分别对应1、2、3、4、5；原有JSON ID和场景数据无需改变。
- 选择该方案是为了减少贴纸感并保留木材质感，同时仍能从外轮廓读取ID和角度；代价是必须在木件加工前完成等比例纸板轮廓测试。
- 详细规格与失败回退条件记录在 [`hardware/CONTOUR_TOKEN_SPEC.md`](../../hardware/CONTOUR_TOKEN_SPEC.md)，机器可读参数位于 `vision/config/contour_tokens.json`。

## 轮廓模板与识别软件预验证

- 新增5个毫米单位SVG切割轮廓和一页A4原尺寸纸板试样PDF；PDF包含100 mm校验线。
- PDF重新渲染后完成视觉检查，无文字缺失、重叠或裁切；程序检查页面为210 × 297 mm，校验线为100.0 mm。
- 新增 `vision/detect_contour_tokens.py`，从矫正画面中提取浅色木片候选，使用外接圆、径向轮廓、大方向缺口和A/B/C编码槽恢复ID与角度。
- 5个ID分别在0°至315°的8个旋转角完成40组识别检查，均在当前8°误差阈值内。
- 综合电子夹具正确识别ID 10、11、12、20、21，并排除95 × 65 px亮色矩形；验证图与JSON保存在 [`docs/images/vision/contour-token-validation/`](../images/vision/contour-token-validation/)。
- 当前结果是合成图软件验证，不替代纸板和真实桦木件测试；`build_layout_packet.py` 尚未切换到轮廓检测后端。

## Token 外观语言探索 V0.1

- 生成A/B/C三组无贴纸造型概念：A为圆角城市铺装节点，B为自然卵石/叶片，C为精简圆片与有机林地。
- Food内部纹样只提示广场铺装和人类活动节点，不使用食物图案；Woodland继续使用毛毡区域加中央刚性木座。
- 概念图中的小圆孔尚未成为编码决定；它们不得在选型前覆盖已验证的方向缺口与A/B/C边缘编码。
- 初步自审推荐组合为“A的Food外形 + C的Woodland外形”：前者最能表达城市规划构件，后者兼顾毛毡质感和中央识别核心。
- 概念图保存在 [`docs/images/tangible-prototype/token-shape-concepts-v01.png`](../images/tangible-prototype/token-shape-concepts-v01.png)，等待造型方向审核后再制作精确矢量版本。

## Token 外观选择与精确化 V0.2

- 学生确认采用“A款Food＋B款Woodland”，替代先前自审建议的A＋C组合。
- Food固定为最大外径60 mm的柔和六边形城市铺装节点；Woodland固定为120 × 90 mm叶片毛毡加50 mm圆形木质识别芯。
- 四个10 × 10 mm磁吸贴片安装在毛毡背面；毛毡与装饰刻线均不参与视觉识别。
- 新增独立V0.2配置和SVG目录，保留V0.1圆形模板作为回退，不在实物测试前覆盖基线。
- V0.2继续使用10 × 7 mm方向缺口和8 × 5 mm A/B/C编码槽，逻辑ID和Unity数据结构不变。
- 5个V0.2 ID分别通过8个旋转角，共40组合成识别检查；综合夹具也正确识别全部5件并排除亮色矩形干扰物。
- 无隐私输入图、候选Mask、标注图和检测JSON保存在 [`docs/images/vision/contour-token-v02-validation/`](../images/vision/contour-token-v02-validation/)。

## V0.2轮廓后端接入统一数据包

- `build_layout_packet.py` 的命令行默认Token后端已从测试用ArUco切换为V0.2轮廓识别；旧模式可显式使用 `--token-backend aruco`。
- 数据包新增 `recognition.token_backend` 与 `recognition.token_config_version`，Unity原有 `tokens` 字段结构不变。
- Confirm现在检查完整Token集合；ID 10、11、12、20、21任一缺失或重复都会返回错误，不写入新的确认布局。
- 新增带四角透视、A款Food、B款叶片毛毡、5个轮廓木件和彩色路径的全链路无隐私夹具。
- 全链路正确输出5个ID、归一化位置、角度和11点连续路径；验证文件位于 [`docs/images/vision/layout-packet-contour-v02-validation/`](../images/vision/layout-packet-contour-v02-validation/)。

## Unity P0输入层

- 使用本机Unity `6000.3.4f1 (fdd3b8998ec4)` 在 `unity/` 建立标准工程，项目版本、Packages和ProjectSettings已纳入版本控制；生成目录继续由 `.gitignore` 排除。
- 新增 `LayoutPacketModels` 与 `LayoutPacketReader`，使用Unity官方Newtonsoft JSON包读取路径二维数组。
- Unity端独立复核协议版本、时间戳、ID 10/11/12/20/21完整且唯一、Token数值范围和连续路径，不直接信任Python的验证标记。
- 创建 `Assets/Scenes/P0_InputSpike.unity`，包含输入管理器与正交摄像机。
- Unity批处理编译成功，并读取轮廓版脱敏夹具；烟雾测试结果为 `UNITY_INPUT_SMOKE_OK tokens=5 path_points=11 backend=contour`。
- 当前尚未把Token和路径实例化为可视场景对象；下一层应建立Environment输入状态与Debug视图，再进入NPC和动物行为。
