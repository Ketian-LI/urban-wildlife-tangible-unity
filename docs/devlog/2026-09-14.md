# 2026-09-14

## Residential visual approval and Unity integration

- Locked `residential-abc-approved-v11.png` as the residential A/B/C reference.
- Kept the complete rounded lawn, entrance path, trees, and shrubs on every lot.
- Exported three transparent Unity sprites for detached, paired, and apartment lots.
- Replaced the previous residential resource paths in the city prototype.
- Updated the visual smoke check to require all three approved landscaped lots.
- Captured a Unity camera preview to verify that the lawn bases remain visible and
  the sprites are not clipped.

## Verification

- RGBA transparency checked on all three PNGs.
- Unity city prototype batch verification passed, including planning, responsive
  camera, construction rules, and the residential A/B/C visual contract.

## Grid-based city construction

- Replaced the coarse 6 × 4 test board with a 9 × 6 logical city grid matching
  10 × 10 cm cells on the 90 × 60 cm physical board.
- Added 1 × 1 detached-house, 2 × 1 commercial/community and 2 × 2 apartment
  footprints, including 90-degree rotation, full-footprint validation and atomic
  demolition.
- Kept the grid visually faint, removed coordinate labels, reduced road widths and
  arranged the six baseline buildings as small connected city clusters.
- Preserved all overwritten woodland IDs and `was_woodland` history across every
  occupied cell.
- Captured `docs/visual-concepts/city-grid/city-grid-unity-preview-v01.png` from the
  Unity camera for visual review.

## Grid verification

- Unity batch verification passed for 54 cells, 36 available cells, multi-cell
  placement, fixed-water rejection, nine-building cap, full-footprint demolition,
  and the complete Scan → Preview → Route → DP → Construction workflow.

## Compact neighbourhood visual correction

- Replaced the sparse experimental underlay with a warm ivory compact-neighbourhood
  base made from many small rounded parcels and separated green pockets.
- Kept buildings, trees, the pond and civic plaza out of the baked ground asset so
  that grid construction and habitat replacement remain visually live in Unity.
- Hid road-route and pedestrian-route overlays by default; the light corridors in
  the ground artwork now carry the normal street reading, while route overlays can
  still be enabled for inspection.
- Reduced commercial/community building scale, centred all building artwork in its
  footprint, reduced vehicle scale and replaced lettered pigeon/squirrel/fox discs
  with their existing animal artwork.
- Saved the generated underlay iterations and Unity camera previews under
  `docs/visual-concepts/city-grid/`.

## Neighbourhood verification

- Unity 6000.3.4f1 batch verification passed again for the complete city prototype,
  including 54-cell construction, responsive split-screen layout, wildlife,
  mobility, route selection, DP and multi-cell demolition.

## Starting-map visual direction

- Removed the surrounding ocean and retained one flat-colour river through the
  right side of the map.
- Reduced the starting settlement to two houses and one naturally curving main
  road that enters and exits the map across a single bridge.
- Restored the approved muted blue/red roof language after rejecting the first
  tiny generic-house draft.
- Wrapped the back and sides of both homes with spaced woodland, keeping the road,
  driveways and front clearings unobstructed.
- Saved the retained concept iterations under `docs/visual-concepts/city-grid/`.

## External vehicle round trips

- Added an `ExternalJourney` trip purpose and an explicit outside-city gateway.
- High vehicle-demand homes now create a traceable representative car journey that
  follows connected roads, crosses the map boundary, waits outside and reverses the
  exact route back home.
- Preserved internal walking trips and did not add random background traffic.

## External vehicle verification

- Unity 6000.3.4f1 city integration verification passed after the mobility change.
- Focused smoke verification passed for two external journeys, including boundary
  exit, outside dwell, returning state and completion at the original home.
