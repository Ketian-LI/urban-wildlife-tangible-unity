# 开发日志：2026-09-09

## 今日目标

把已经通过软件闭环验证的动物行为换成更接近早期网页Demo的俯视手绘显示语言，同时不改变P0状态机、约束和日志数据。

## 已完成

- 对照早期网页Demo，提取其关键运动语言：2D动物图像、约0.62秒的位置过渡、约0.42秒的Sprite过渡和约6.8–8.2秒的轻微待机摆动。
- 使用内置图像生成工具分别制作透明背景的鸽子、欧亚红松鼠和城市赤狐俯视Sprite；完整提示词与来源说明保存在`unity/Assets/Resources/UrbanWildlife/Animals/README.md`。
- 新增统一Sprite导入规则，关闭mipmap、保留透明度、使用双线性过滤并限制原型纹理尺寸。
- 将动物Sprite接入现有`P0AnimalSimulation`，保留原状态机与路线，只增加平滑转向、约7.2秒待机摆动、进食脉动及进食/回避/撤退状态色。
- 根据第一次屏幕检查把三种动物的地图显示长度调整为鸽子0.65、松鼠0.85、狐狸1.05 Unity单位，使其在9 × 6单位地图上可读但不会遮挡主要规划元素。
- 在Unity批处理烟雾测试中加入三个资源的加载、边界与纹理检查。

## 研究与范围说明

- 此次变化只影响表现层，不修改动物对Food Hotspot、Woodland和Human Activity的响应规则。
- 程序动画用于P0交互节奏与可读性验证，不代表真实物种步态或精确生态行为。
- 生成图像是V0.1原型素材；最终提交前需按课程要求复核AI素材使用、作者声明和来源记录。

## 下一项

在Unity编辑器中复核地图、人物、动物、规划标记和紧凑信息卡的整体可读性；材料到货后继续固定俯拍条件下的P0实体Vertical Slice。Trace仍需等待该闭环稳定后进入。

## 视觉系统V0.2追加

在动物V0.1之后继续完成统一视觉层：

- 生成并接入3:2手绘S001公园底图，树木和花坛集中在边缘，中心保留规划空间。
- 第一次底图包含过大的池塘和固定长椅等会与未来P1输入混淆的道具，因此没有直接采用；通过两次定向编辑缩小池塘、删除长椅/灯柱/石柱，并调整广场位置。
- 背景图只提供视觉气氛；入口A、出口B、20 × 14 cm广场和18 × 12 cm池塘继续以场景JSON坐标生成精确边界，避免视觉与判定不一致。
- 分别生成Walker、Dweller和Visitor俯视人物。初始输出把透明棋盘格画进了RGB，因此没有直接使用；经背景提取后二次检查，三张最终PNG均具有真实Alpha通道。
- 三类人物通过蓝灰背包、芥末黄外套与杯子、青绿雨衣与斜挎包区分，并在运行期加入平滑转向、行走起伏、停留呼吸和观察摆动。
- 规划路径增加深色描边；Food显示为编号六边形，Woodland显示为叶片毛毡加木芯；布局根对象改为7个语义元素并显式释放运行时Mesh与Material。
- 控制面板缩小；Run与Observe自动切换为紧凑信息卡，完整约束仍保留在Plan与Confirm。
- Unity批处理全流程验证通过，新增`UNITY_VISUAL_LAYER_SMOKE_OK human_sprites=3 map_sprites=1 semantic_elements=7`。

完整图像生成提示词、编辑链和来源说明分别保存在环境与人物素材目录。视觉素材仍为原型，最终提交前需复核课程对AI辅助素材的声明要求。

## 人物鞋子姿态修正 V0.2.1

- 屏幕复核发现蓝衣Walker与青蓝衣Visitor采用了过强的俯视透视：迈出的前脚位于头顶上方，缩小为地图角色后产生错误身体读向。
- 两张人物图只重绘腿脚姿态，保留服装、背包/挎包、色彩和角色身份；修正版中两只鞋均位于髋部下方，步幅收小且与腿连续连接。
- 新文件保存为`walker-topdown-v02.png`与`visitor-topdown-v02.png`，V0.1文件保留用于过程记录，运行时与烟雾测试已切换到修正版。
- 新增人物轮廓规则：头部必须处于源图上方，两只鞋必须完整位于髋部下方；在地图显示尺寸下不得出现鞋高于头部、悬空或断肢的读形。
- 两张V0.2文件均为1024 × 1536 RGBA，四角完全透明；新资源GUID无重复，旧素材保留。
- 使用实际地图显示组件在Unity图形模式重新渲染，组合预览保存为`docs/images/unity/visual-layer-v021-shoe-fix-preview.png`。该图为摆放人物位置后的静态可读性检查，不代表新增的动画或实体输入验收。
- 回归检查：Python 37/37通过；Unity完整烟雾测试通过，包含三类人物资源加载、人类/动物状态、研究日志和三周期流程，退出码0。第一次截图使用`-nographics`触发渲染崩溃，改用图形模式后截图成功。

## 显示层精修 V0.3

- 根据第二次人物检查，把Walker和Visitor的两双鞋由“鞋尖朝画面下方”改为与脸同向：鞋尖朝源图上方，画面下端显示后跟、后跟拉片和后侧鞋底。修正版保存为`walker-topdown-v03.png`和`visitor-topdown-v03.png`。
- Food Hotspot继续使用与实物轮廓一致的六边形，但增加深色底、奶油色边、橙色面、六个活动节点和中心编号铭牌，使其表示人类活动形成的资源可用性，而不是直接食物。
- Woodland继续使用叶片毛毡与圆形木芯结构，增加深浅毛毡、三块冠层、叶脉、树皮圈、木心和年轮；占地与识别规则没有变化。
- 入口A和出口B保留原0.6 × 0.8 Unity单位判定框，增加双门柱、弧形门槛与ENTRY/EXIT标识；路径仍可视为从门内连续通过。
- 松鼠改成紧凑圆身与左侧C形蓬松尾巴；狐狸强化细长身躯、尖耳、黑腿和末端四分之一白尾尖。两者透明轮廓宽高比约为0.73与0.20，缩小后仍有明显差异。
- 新组合预览保存为`docs/images/unity/visual-layer-v03-refinement-preview.png`。Unity烟雾测试新增门、活动节点、叶脉存在性检查，以及松鼠/狐狸宽高比至少2倍的轮廓差异检查。
- 最终回归通过：Python 37/37；Unity完整流程退出码0，实际透明轮廓宽高比为松鼠0.73、狐狸0.20；四张新增Sprite均为1024 × 1536 RGBA且四角透明，Unity资源GUID无重复。
