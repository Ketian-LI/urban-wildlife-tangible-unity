# City board existing streets V0.6 — reference tree correction

Generated on 2026-09-15 with the built-in image generation tool in `precise-object-edit` mode. Image 1 was the V0.5 road map edit target; Image 2 was the user-approved Riverside Park visual reference for tree shape, scale, palette, and lightness only.

Selected Unity asset:

`unity/Assets/Resources/UrbanWildlife/Environment/city-board-woodland-existing-streets-v06.png`

Final prompt:

> Replace only the trees in Image 1 so they use the same clean, simplified Riverside Park tree language as Image 2. Remove the current dark, swollen, semi-realistic multi-lobed tree crowns. Redraw all trees as a restrained family of exactly five reusable silhouettes: slender teardrop canopy, soft oval canopy, compact round canopy, small sapling, and a sparse two-canopy cluster. Use thin short brown trunks; smooth sage-to-muted-emerald foliage; very soft shallow shading; high lightness, moderate-low saturation; minimal cast shadow. Most mature trees should share a consistent size, saplings about 70–75% of that size, with no oversized trees. Maintain visible gaps between neighbouring trees so each silhouette reads separately. Preserve the existing overall tree positions and forest density pattern, but simplify clumps that currently merge into one dark mass. Preserve the exact framing, every road, every woodland boundary, all warm-white ground, and the no-water layout. Change only tree artwork; add no buildings, people, vehicles, animals, paths, roads, water, text, UI, grid, border, or watermark.

Prototype provenance note: this is an AI-assisted visual asset. Confirm the course policy and include the chosen disclosure in final submission material.
