# 2026-09-15 开发记录

## 活动热点图

- 移除地图上的逐点鞋印、车辆轮迹、鸟爪和兽爪，避免运行一段时间后地图被大量符号覆盖。
- 保留Observation中的Human与Animal精确采样以及研究日志，不改变行为模拟、统计或阶段报告数据。
- 将可见层改为18 × 12聚合热点图；同一区域的样本合并，低、中、高强度使用蓝、黄、红色渐变和三层柔和光晕表达。
- 热点图默认隐藏，运行中按 `H` 即时显示或关闭；右侧面板也提供同一开关，并保留People、Wildlife、All三种筛选。
- 隐藏状态只缓存最新样本，不创建热点几何；打开或切换筛选时才重建，避免模拟每帧产生无意义的视觉开销。

## 验证

- Unity批处理烟雾检查通过：热点图默认隐藏，开关有效，同一网格样本正确聚合，People/Wildlife/All筛选正确。
- 检查生成层中不存在鞋印、轮胎印、爪印或脚趾几何，完整城市原型集成检查同时通过。
